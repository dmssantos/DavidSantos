import styled from "styled-components";

const WapperTitle = styled.div`
  width: 193px;
  height: 51px;

  border-radius: 64px;
  border: 1px solid var(--primary);

  display: flex;
  align-items: center;
  justify-content: center;

  position: absolute;
  top: 130px;

  font-size: 32px;
`;

export default WapperTitle;
